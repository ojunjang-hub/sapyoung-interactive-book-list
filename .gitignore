# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# 환경 설정 (API 키 포함)
.env

# DB (로컬 전용 — 서버 이식 시 별도 마이그레이션)
books.db
*.db-shm
*.db-wal

# 스토리지 (업로드 파일) — config FILE_STORAGE_PATH=./storage 기준 루트 경로
storage/

# 로컬 표지 이미지 (대용량·재생성 가능: books.db의 cover_remote_url로 재다운로드)
# 서버 이식 시 books.db처럼 별도 전송하거나 백필 스크립트로 재생성한다.
public/covers/

# 작업 디렉토리 (스크립트 중간 산출물)
_workspace/

# QA 세션 작업물 (대용량 DB 백업·검토 CSV)
QA_세션_작업물/

# 루트 일회성 보강 스크립트 (API 키 포함 가능 — 커밋 금지)
/aladin_enrich.py
/kobic_enrich.py
/수정 작업 6건.txt

# 에디터
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
.DS_Store

# 임시/로그
uvicorn.log
*.log
*.tmp.*

# InDesign 원본 (대용량 바이너리)
*.idml

# Claude Code 로컬 설정
.claude/
