# 도서목록 웹 — 첫 화면 구조 (IA) 명세

> 첫 화면을 **3개 섹션**으로 구성한다. 882권을 한 번에 펼치지 않고, 두 가지 찾기 경로(안내받기·직접 묻기)와 신간 흐름만 둔다. 전체 그리드는 첫 화면 섹션이 아니라 "결과·전체보기" 목적지로 한 층 아래에 둔다.
>
> 본 명세는 현재 빌드 §1~7(그리드·필터·상세·관리자·AI검색 완료) 위에 첫 화면을 재구성하기 위한 것이다. 와이어프레임은 채팅의 `first_screen_three_sections` 참고.

---

## 1. 화면 구성 (3 섹션)

```
[헤더]  사회평론 도서목록            전체 882권 · 관리자
────────────────────────────────────────────
섹션 1  ▸ 어떤 책을 찾으세요?   (가지치기로 안내받기)
섹션 2  ▸ 프롬프트 입력창 + 예시 2~3개  (직접 물어보기)
섹션 3  ▸ 신간 표지 흐름 (가로 스크롤)
```

섹션 1·2는 모두 **기존 그리드 결과 화면**으로 수렴한다.

---

## 2. 섹션 1 — 가지치기 파인더 ("어떤 책을 찾으세요?")

질문에 답하며 단계별로 후보를 좁혀 마지막에 그리드(필터 적용 상태)로 도착한다. **각 단계 = 필터 한 겹**이므로, 가능한 한 기존 필터 데이터에 매핑한다.

### 2.1 결정 트리 (2단, 확정)

```
Q1. 누구를 위한 책인가요?
    ├─ 나를 위한 책      → 부서: 교양
    ├─ 아이와 함께        → 부서: 어린이
    └─ 공부·연구용        → 부서: 아카데미
            │
            ▼
Q2. 어떤 주제인가요?
    └─ 선택한 갈래(부서)의 분야 목록을 동적으로 제시
       (DB의 distinct category_effective 값에서 생성)
            │
            ▼
결과. department + category 필터를 적용한 기존 그리드로 이동
```

### 2.2 대상 → 부서 매핑 (기본값, 조정 가능)

| Q1 선택 | department_effective |
|---|---|
| 나를 위한 책 | 교양 |
| 아이와 함께 | 어린이 |
| 공부·연구용 | 아카데미 |

- **백리스트(124권) 처리**: 부서로는 별도지만 성격상 주제 교차(구간 전반)다. 기본 방침은 Q2 주제 필터를 **부서 교차로 적용**해, 같은 분야의 백리스트 도서도 결과에 함께 노출. (대안: 백리스트를 '나를 위한 책' 결과에 합류. 택1 — §6 미결정)

### 2.3 구현 메모

- 새 백엔드 거의 불필요. Q2 분야 목록은 `GET /api/filters`, 결과는 `GET /api/books?department=&category=` 재사용.
- 신규는 **프런트의 단계형 파인더 UI**와 **대상→부서 매핑 테이블**뿐.
- 모바일에서도 한 손으로 누르며 내려가도록 큰 선택 버튼.

### 2.4 난이도 축 (보류)

- "입문 ↔ 심화" 같은 난이도 단계는 **현재 데이터에 없음** → 1차 버전 제외.
- 추후 추가 시: 목차·소개 기반으로 Claude가 난이도 1차 분류 → 관리자 보정(오버라이드 방식) → 트리에 3단계로 삽입.

---

## 3. 섹션 2 — 프롬프트 검색 ("직접 물어보기")

- 입력창 1개 + 아래 **예시 프롬프트 2~3개**(클릭 시 자동 입력·실행).
- 동작: 기존 §7 `POST /api/search/natural` 호출 → 반환된 ISBN들을 그리드로 표시.
- 예시 프롬프트 기본값(조정 가능): "국어 교육 기초서", "처음 읽는 철학", "아이 그림책 추천".
- (선택) 예시 프롬프트를 관리자 화면에서 관리 가능하게 하면 시즌별 교체가 쉬움.

---

## 4. 섹션 3 — 신간 흐름

- 데이터: 출간일 내림차순 상위 N권(예: 12권).
- 표시: 가로 스크롤 표지(`cover200` 썸네일), 클릭 → `book.html?isbn=ISBN13`.
- 엔드포인트: `GET /api/books?sort=pub_date_desc&limit=N` (정렬·limit 파라미터 없으면 추가).

---

## 5. 내비게이션 모델

- **헤더**: 로고(사회평론 도서목록) / "전체 882권"(→ 전체 그리드) / 관리자(→ admin.html).
- **수렴 지점**: 섹션 1(트리 완료)과 섹션 2(검색)는 모두 기존 그리드 결과 화면으로 이동(필터·검색 상태 반영). 그리드는 첫 화면에 노출하지 않고 결과·전체보기로만 진입.

---

## 6. 재사용 / 신규 구분 (Claude Code 작업 가이드)

**재사용 (이미 있음)**

- AI 자연어 검색: `POST /api/search/natural` (§7)
- 그리드 + 필터 화면, `GET /api/books`, `GET /api/filters` (§4)
- 상세 페이지 `book.html` (§5)

**신규 (이번 작업)**

- 첫 화면 레이아웃(3 섹션) — `index.html`/`main.js` 재구성
- 섹션 1 단계형 파인더 UI + 대상→부서 매핑
- 신간 정렬·limit 파라미터(필요 시 `books.py`에 추가)
- 섹션 2 예시 프롬프트(하드코딩 또는 관리자 관리)

---

## 7. 보류 / 후속 (§8 연계)

- 난이도 태그(AI 분류 + 관리자 보정)
- 연관 도서 매핑(상세 페이지)
- 읽는 순서 제안(Claude 큐레이션)
- MBTI 도서 추천(재미 요소)

---

## 8. 미결정

- [ ] 백리스트 처리 방식 확정(주제 부서교차 vs '나를 위한'에 합류)
- [ ] 신간 표시 권수 N
- [ ] 섹션 2 예시 프롬프트 최종 문구
- [ ] 대상→부서 매핑 최종 확인(특히 교양/아카데미 경계 도서)
