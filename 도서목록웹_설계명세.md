# 사회평론 도서목록 웹페이지 — 설계 명세 (v2)

> 매년 인쇄하는 414쪽 종이 도서목록을 인터랙티브 웹으로 전환한다.
> 본 프로젝트는 **도서 마케팅 보조 AI와 데이터 정본을 공유**한다. 도서목록 웹을 먼저 구축하되, 마케팅 AI가 나중에 같은 데이터 위에 얹힐 수 있도록 데이터 층을 설계한다.
>
> **v2 변경점**: 관리자 분류 편집 / 도서별 자료 업로드·다운로드 / Claude API 자연어 검색 / 인터랙티브 요소가 추가되며, 순수 정적 사이트에서 **정적 프런트 + 가벼운 백엔드** 구조로 전환된다.

---

## 1. 배경과 목적

- 회사에서 매년 새로 인쇄하는 도서목록이 있다(414쪽, 큰 판형). 인쇄·제본 비용이 크고, 찍는 순간 고정되어 연중 신간·절판·중쇄를 반영하지 못한다.
- 종이목록의 약점이 그대로 웹의 강점으로 바뀐다: 검색·필터, 표지 그리드, 책별 상세, 구매 링크, 데이터만 갱신하면 항상 최신.
- **용도: 사내 참고용 + 외부 공개용 둘 다.**
- 데이터는 현재 인디자인/한글 조판 원본만 있다. 일단 기존에 편집하던 도서목록 데이터를 쓴다.

---

## 2. 범위

**구현 목표**

- 표지 썸네일 그리드 + 검색 + 필터 + 정렬
- 책별 상세 페이지(서지정보 + 소개 + 표지)
- 외부 서점 구매 링크
- **부서/분야 분류 + 관리자 편집** (§5)
- **도서별 자료 업로드/다운로드** (§6)
- **Claude API 자연어 검색** (§7)
- **인터랙티브 요소** (§8) — 핵심 카탈로그 가동 후 단계적 추가

**제외 / 후속**

- 매년 조판 원본에서 자동 추출·갱신하는 완전 자동 파이프라인(후속)
- 도서 마케팅 보조 AI(후속, 별도 명세). 단, 본 명세는 그 데이터를 공유 가능하게 설계한다.

---

## 3. 두 프로젝트의 관계 — 공유 데이터 정본

도서목록 웹과 마케팅 AI는 **하나의 도서 데이터 정본(source of truth)** 을 공유한다.

```
        [ 도서 데이터 정본 (DB: SQLite/Postgres) ]
            ↑ 만든다 (본 프로젝트)
        ┌───────────────┴───────────────┐
   도서목록 웹                      마케팅 보조 AI (후속)
   (카탈로그 렌더링)                  (검색 + 카피 생성)
```

**원칙**

- 데이터 스키마는 두 프로젝트를 모두 만족하는 **상위집합(superset)** 으로 설계한다. 웹은 그 일부만 화면에 쓴다.
- 데이터 추출·보강 파이프라인은 **한 번만** 만들어 양쪽이 재사용한다.
- 알라딘 API 보강 시, 웹이 당장 쓰지 않는 필드(목차·책속인용구 등)도 **이번 보강 패스에서 함께 적재**한다. 알라딘 일일 호출 한도가 있으므로 한 번에 풀세트를 받아두면 마케팅 AI 단계에서 재호출이 필요 없다.

---

## 4. 아키텍처

**v1의 "순수 정적 사이트"에서 전환.** 관리자 편집(§5)·파일 업로드(§6)·서버측 AI 호출(§7)이 추가되어 가벼운 백엔드가 필요하다.

```
[공개용 프런트 (정적/캐시, 빠름)]  ──읽기──>  [데이터 정본 DB]
                                                  ▲
[관리자 화면 (인증 필요)] ──쓰기──> [백엔드] ──> [DB + 파일 Storage]
                                       │
사용자 자연어 질의 ──> [백엔드 = Claude API 프록시] ──> Claude API
```

### 4.1 백엔드: 사내 서버 목표 + 로컬 우선 개발 (확정)

- **최종 배치는 사내 서버.** 데이터를 사내에 두기 위함. 단, 서버 프로비저닝은 **사내 서비스팀 문의 대기** 중.
- **당장은 본인 PC(로컬)에서 개발**하고, 이후 사내 서버로 이전한다.
- 따라서 스택은 **로컬(Windows)에서 돌아가고 리눅스 사내 서버로 그대로 이식되는** 자체 호스팅 구성으로 잡는다. (BaaS는 채택하지 않음 — 최종 목표가 사내 서버이므로)

**권장 스택 (이식성 우선)**

| 구성 | 선택 |
|---|---|
| 백엔드 | Python **FastAPI** (또는 Node/Express) — 단일 프로세스, Windows↔리눅스 동일 동작 |
| DB | **SQLite** (파일 DB, 그대로 복사 이전) → 사내 서버 규모 확장 시 Postgres 전환 가능 |
| 파일 Storage | 로컬 폴더 (경로를 **환경설정 변수**로 두어 서버 이전 시 경로만 교체) |
| AI 프록시 | 같은 백엔드 안에서 Claude API 호출 (키는 `.env`로 분리) |
| 프런트 | 정적 HTML/JS — 백엔드가 함께 서빙 |

> **이식 원칙**: DB는 SQLite 파일, 파일 경로·API 키·포트는 모두 환경설정으로 분리. 사내 서버 이전 = 코드 + SQLite 파일 복사 + `.env` 교체로 끝나도록 설계.

> 분리 구조: 공개 카탈로그 화면은 정적/캐시로 빠르게, **동적인 부분(관리자 CRUD·파일·AI검색)만 백엔드**가 담당.

### 4.2 데이터 저장

- 정본은 **DB(SQLite 또는 Postgres)**. 관리자 편집·이력 관리를 위해 평면 JSON 대신 DB를 쓴다.
- 공개 프런트용으로는 DB에서 정적 JSON을 빌드·캐시해 빠르게 서빙할 수 있다.

---

## 5. 분류 체계 (부서/분야) + 관리자 편집

### 5.1 추출

- 도서목록은 부서·분야 단위로 묶여 편집되므로 **레이아웃 구조 자체가 분류 정보**다(섹션 제목, 페이지 묶음).
- 이 분류는 **회사 고유 분류**이므로 알라딘이 아니라 **도서목록에서 추출**한다(알라딘 카테고리와 별개로 보관).
- 추출 시 각 책이 "어느 부서 / 어느 분야 섹션 아래 있었는지"를 함께 기록한다.

### 5.2 관리자 편집 (override 방식)

- 관리자 화면에서 책별 부서·분야를 수정할 수 있다.
- **추출 원본 분류와 관리자 수정값을 둘 다 보관**한다. 매년 도서목록을 새로 추출해도 사람이 손본 분류가 덮어써지지 않게 한다.
  - `department_src` / `category_src` (추출 원본)
  - `department` / `category` (유효값 = 수정값이 있으면 그것, 없으면 원본)

---

## 6. 도서별 자료 업로드/다운로드

- 책 1권 : 자료 N개(1:다) 구조.
- 자료 메타: `{종류(ppt/정오표/보충자료/기타), 파일명, 설명, 버전, 업로드일}`
- **관리자만** 업로드·삭제(인증 필요). 사용자는 상세 페이지에서 다운로드.
- 정오표처럼 갱신되는 자료는 버전 표시.
- 파일은 백엔드 Storage(BaaS Storage 또는 NAS 파일 폴더)에 저장.

---

## 7. Claude API 자연어 검색

### 7.1 판단: 현실적이며 잘 맞는다

카탈로그가 1000여 종으로 작다는 점이 결정적. 단계적으로 확장 가능.

### 7.2 구현 방식 (작게 시작 → 필요 시 확장)

1. **(권장 시작) 압축 인덱스 + Claude**: 전 도서의 압축 인덱스(제목 + 한 줄 소개 + 분야, 책당 ~30토큰 → 1000종 ≈ 3만 토큰)를 Claude에 통째로 전달. 질의에 적합한 도서 목록 + 추천 이유를 받음. 벡터 DB 불필요. Claude Code로 즉시 구축. 검색·랭킹은 경량 모델(예: Haiku 계열)로 비용 절감.
2. **(확장) 임베딩 + 재정렬 하이브리드**: 트래픽 증가 시 전 도서를 벡터화해 근접 검색으로 후보를 좁히고 Claude가 재정렬·설명.

### 7.3 주의

- **API 키는 반드시 서버 측에서 호출**(클라이언트 노출 금지). §4 백엔드가 Claude API 프록시 역할을 겸한다.
- 현재 API 모델·요금·한도는 도입 시점에 확인.

---

## 8. 인터랙티브 요소 (종이가 못 하는 것)

핵심 카탈로그(그리드+검색+상세) 가동 **후** 단계적으로 추가한다.

| 기능 | 구현 난이도 | 방식 |
|---|---|---|
| 연관 도서 매핑 | 낮음 | 같은 시리즈·저자·분야 + (임베딩 시) 의미적 유사 → 네트워크 그래프 |
| 읽는 순서 제안 | 중간 | Claude가 한 주제 책 묶음을 입문→심화로 정렬 |
| MBTI 도서 추천 (재미) | 낮음 | 간단 퀴즈 → 유형별 추천 매핑 테이블 (원하면 Claude로 추천 문구 생성) |

---

## 9. 데이터 모델

권당 1레코드. `웹` = 웹 사용, `예약` = 지금 적재하되 마케팅 AI용 보관.

| 필드 | 출처 | 용도 |
|---|---|---|
| isbn13 | 도서목록(조인 키) | 공통 |
| title | 도서목록/알라딘 | 웹 |
| author | 알라딘 `author` | 웹 |
| publisher | 알라딘 `publisher` | 웹 |
| pub_date | 알라딘 `pubDate` | 웹 |
| department_src / department | 도서목록 추출 / 관리자 수정 | 웹(필터) |
| category_src / category | 도서목록 추출 / 관리자 수정 | 웹(필터) |
| aladin_category | 알라딘 `categoryIdList` | 예약(참고) |
| series | 알라딘 `seriesInfo` | 웹(필터·연관) |
| price_standard / price_sales | 알라딘 | 웹 |
| cover_url | 알라딘 / 자체 파일 | 웹 |
| description | 알라딘 `description` | 웹(요약) |
| store_links | ISBN으로 생성 | 웹(상세) |
| stock_status | 알라딘 `stockStatus` | 웹(절판 표시) |
| attachments[] | 관리자 업로드 | 웹(다운로드) |
| toc | 알라딘 `Toc` | 예약 |
| full_description | 알라딘 `fulldescription` | 예약 |
| publisher_description | 알라딘 `fulldescription2` | 예약 |
| story | 알라딘 `Story` | 예약 |
| quotable_phrases | 알라딘 `phraseList` | 예약 |
| sales_point | 알라딘 `salesPoint` | 예약(홍보 우선순위) |
| best_rank | 알라딘 `bestSellerRank` | 예약 |
| embedding | 생성(검색·연관용) | 예약/선택 |

> 마케팅 AI 수동 필드(핵심 메시지·타깃 독자·왜 지금·마케팅 앵글)는 스키마에 자리만 비워둔다.

---

## 10. 데이터 파이프라인

```
1) 조판 원본에서 최소 추출 (제목 + ISBN + 부서/분야 분류, 가능하면 저자·가격)
        ↓
2) 알라딘 ItemLookUp 풀세트 보강 (ISBN13 권당 호출)
        ↓
3) 외부 서점 링크 생성 (ISBN URL 패턴)
        ↓
4) 정본 DB 적재 → 공개용 정적 JSON 빌드
```

### 10.1 조판 원본 추출 (가장 큰 난관) — 인디자인/IDML 확정

조판 원본은 **인디자인**으로 확정. 파일은 "데이터"가 아니라 "그림"이라 기계는 어느 게 저자·가격인지 모른다. **추출 범위를 최소화**해 난이도를 낮춘다 — 제목·ISBN·분류만 확실히 건지면 나머지는 알라딘이 채운다.

- **경로**: `.indd` → 인디자인에서 **IDML로 내보내기**(이 변환은 편집자가 수동 수행) → IDML은 zip 안 XML 묶음이라 **Python(unzip + lxml)** 으로 파싱.
- 도서목록은 한 책 = 반복 레이아웃이므로 그 패턴을 잡아 자동 추출. **분류(부서/분야)는 섹션 구조**에서 추출.
- 대안: JSX 스크립트로 인디자인 내부에서 텍스트프레임을 순회해 CSV로 직접 추출하는 방법도 가능(IDML 파싱이 까다로울 경우 비교 검토).
- **ISBN을 확실히 건지는 것**이 이후 전 과정(보강·표지·서점 링크)을 좌우한다.

### 10.2 알라딘 보강

- `ItemLookUp` (`ItemIdType=ISBN13`, `Output=JS`, `Version=20131101`)
- `OptResult` 풀세트: `Toc, Story, fulldescription, fulldescription2, phraseList, authors, ratingInfo, bestSellerRank, categoryIdList`
- 권당 호출이라 "검색·리스트 200건 제한"에 안 걸림.
- TTBKey 일일 한도 → 최초 1회 전량 적재 후 캐시, 이후 증분.

### 10.3 외부 서점 링크 / 표지 / 이용 범위

- 서점 링크: 교보·예스24·알라딘 검색 URL을 ISBN으로 자동 생성.
- 표지: 초기엔 알라딘 `cover_url`, 외부 공개 안정성을 위해 자체 표지 파일로 전환 권장.
- 이용 범위: 책 소개·출판사 소개는 무난하나, **리뷰·평점 등 알라딘 자체 데이터를 외부 공개에 재게시하는 건 약관 확인** 필요. 외부 공개 화면에는 안전 필드만 노출.

---

## 11. 웹 화면

- **목록(그리드)**: 표지 썸네일, 검색(제목·저자), 필터(부서·분야·연도·저자·가격대·시리즈), 정렬.
- **상세 페이지**: 표지, 서지정보, 책 소개, 외부 서점 링크, 자료 다운로드 목록, 연관 도서, 절판/품절 표시.
- **자연어 검색 입력창** (§7).
- **사내/외부 구분**: 외부 공개 화면엔 공개 안전 필드만. 내부 메모·재고 상세 등은 비표시/관리자 전용.

---

## 12. 기술 스택 (확정)

- 구현: **Claude Code** 중심 (Windows / PowerShell 환경).
- 추출: **Python**(IDML = zip + lxml 파싱). 인디자인 IDML 내보내기는 수동.
- 백엔드: **Python FastAPI + SQLite + 로컬 파일 폴더** (Node/Express도 가능). 로컬 개발 후 사내 리눅스 서버로 이식.
- AI 프록시: 백엔드 내 Claude API 호출(`.env`로 키 분리).
- 프런트: 정적 HTML/JS(바닐라 또는 경량 라이브러리), 백엔드가 함께 서빙.
- 데이터: SQLite 정본 + 공개용 정적 JSON 빌드.
- 형상관리: GitHub(`ojunjang-hub`).

---

## 13. 빌드 순서

1. 조판 원본 샘플 2~3권으로 **추출 스크립트 1차 버전**(제목+ISBN+분류 확보 확인).
2. 확보한 ISBN으로 **알라딘 ItemLookUp 보강** 시험.
3. 추출+보강 전 도서 확장 → **정본 DB 생성**.
4. 정본을 읽어 **그리드 + 검색 + 필터** 화면 구현. ← 여기서 첫 가시적 성과.
5. **상세 페이지 + 외부 서점 링크** 추가.
6. **관리자 화면**(인증) + 분류 편집 + 자료 업로드/다운로드.
7. **Claude API 자연어 검색** 추가.
8. **인터랙티브 요소**(연관 도서 → 읽는 순서 → MBTI) 단계적 추가.
9. 표지 자체 파일 전환, 외부 공개 필드 정리, 배포(NAS/호스팅).

---

## 14. 마케팅 AI 연결 지점 (forward-compat)

- 정본 DB를 마케팅 AI가 그대로 읽는다 → 별도 데이터 구축 불필요.
- 보강 패스에서 목차·소개·책속인용구·판매지수·베스트셀러 순위까지 적재 → 마케팅 AI 단계에서 알라딘 재호출 없이 두꺼운 카드 원재료 확보.
- 임베딩 필드는 연관 도서·자연어 검색·마케팅 검색에 공통 활용.
- 수동 마케팅 필드는 스키마에 자리만 비워둠.

---

## 15. 미결정 / 다음 단계

**확정됨**: 조판 원본 = 인디자인(IDML 경로) · 백엔드 = 자체 호스팅(로컬 우선 개발 → 사내 서버 이전).

- [ ] 인디자인 IDML 내보내기 + 샘플 2~3권으로 추출 스크립트 1차 버전(제목·ISBN·분류 확보 확인)
- [ ] (대기) 사내 서버 프로비저닝 — 사내 서비스팀 문의
- [ ] 알라딘 TTBKey 발급 및 일일 한도·약관 확인
- [ ] 외부 공개 / 비공개 필드 경계 확정
- [ ] 관리자 인증 방식 결정
- [ ] 자연어 검색 시작 방식 확정(압축 인덱스 → 임베딩 확장 시점)
- [ ] 표지 자체 파일 확보 경로
- [ ] 사내 서버 이전 시점·절차(코드 + SQLite + `.env`)
