# 데이터 보강·추출 보정 지시서

> 목적: 목차·상세 누락과 분야 분류 누락을 바로잡는다.
> **원칙: 처음부터 재작업이 아니다.** 추출·보강 단계만 국소 재실행하며, **DB 스키마·웹·관리자 화면·AI 검색은 그대로 둔다.** `admin_overrides`(사람이 수정한 분류)는 절대 건드리지 않는다.

---

## 수정 A — 알라딘 API 보강 (목차·상세) **[먼저]**

목차·상세는 스크래핑 대상이 아니라 **API가 원래 주는 데이터**다. 누락 원인은 십중팔구 `OptResult` 미설정 또는 응답 파싱 누락이다.

### A-1. OptResult 풀세트로 호출

`ItemLookUp` 호출에 아래를 포함:

```
ItemIdType=ISBN13
Output=JS
Version=20131101
OptResult=Toc,Story,fulldescription,fulldescription2,phraseList,bestSellerRank,categoryIdList,ratingInfo
```

> 마케팅 AI 대비 차원에서 `phraseList`(책속에서)·`bestSellerRank`·`categoryIdList`까지 한 번에 받아 적재한다(알라딘 일일 한도 절약 — 한 번에 풀세트).

### A-2. 응답 파싱 위치 (실제 JSON으로 확인 후 매핑)

| 데이터 | 추정 응답 경로 | DB 칼럼 |
|---|---|---|
| 목차 | `item[0].subInfo.toc` | `toc` |
| 줄거리 | `item[0].subInfo.story` | `story` |
| 상세 소개 | `item[0].fullDescription` | `full_description` |
| 출판사 상세 | `item[0].fullDescription2` | `publisher_description` |
| 책속에서 | `item[0].subInfo.phraseList` | `quotable_phrases` |

> 경로는 실제 응답을 한 번 덤프해 **확인 후 확정**할 것(API 버전에 따라 키가 다를 수 있음). 추측으로 매핑하지 말 것.

### A-3. 검증 우선 (전체 재호출 전에)

ISBN 한 권으로 먼저 테스트한다:

```
검증 ISBN: 9788956025513  (행복의 정복)
기대: subInfo.toc 가 비어 있지 않음, fullDescription 채워짐
```

- 채워지면 → **스크래핑 없이** A-1 설정으로 전 권 재보강. 끝.
- 일부 책에서 `toc`가 비면 → 그 책들만 수정 B(스크래핑)로 보충.

### A-4. 호출 한도

최초 1회 전량 재보강 후 로컬 캐시. 이후 신간·갱신분만 증분 호출. 키는 `.env`로만 관리(README/저장소에 노출 금지).

---

## 수정 B — 스크래핑 폴백 (API가 비는 잔여분만) **[조건부]**

수정 A로 목차·상세가 채워지면 **이 단계는 건너뛴다.** API `toc`가 빈 책에만 적용.

### 현재 스크래핑이 망가진 원인

| 증상 | 원인 |
|---|---|
| `책소개`에 목차가 섞임 | 목차를 전용 요소가 아니라 본문 덩어리째 긁음 |
| `### 목차`가 항상 `_없음_` | 목차 선택자가 잘못된 곳을 가리킴 |
| `출판사 제공`이 중복됨 | "더보기/접기"의 접힌 미리보기 + 펼친 전체를 둘 다 수집 |
| 내비 텍스트 혼입 | "도서 모두보기·신간알림 신청·최근작·대표분야" 등 제거 안 함 |

### 수정 지침

1. **필드별 전용 컨테이너를 선택자로 직접 지정.** 알라딘 상세 페이지는 책소개·목차·출판사 서평이 각각 다른 div다. 화면에 보이는 큰 덩어리를 통째로 읽지 말 것.
2. **더보기/접기 처리.** "더보기"를 클릭해 펼친 뒤 **펼쳐진 노드만** 읽거나, 숨은 전체 텍스트 노드만 읽는다. 미리보기 + 전체를 합치지 말 것(중복 원인).
3. **보일러플레이트 필터.** "도서 모두보기", "신간알림 신청", "최근작 :", "대표분야 :", "더보기", "접기", "도서 모두보기" 등은 제거.
4. **책소개/목차 분리.** 책소개 필드에서 목차성 텍스트(단원·장 목록)를 분리해 `toc`로 보낸다.
5. 결과는 A의 DB 칼럼과 동일 스키마로 적재(별도 칼럼 만들지 말 것).

---

## 수정 C — IDML 추출기 분야 인식 (분류 백필)

1차 추출이 부서는 잡았으나 **분야 섹션(심리·국어·고고학·미술사 등)을 누락**했다. IDML 파일은 그대로 있으므로 추출기만 고쳐 `category`를 백필한다.

### 로직 (상태 머신)

1. 도서목록 IDML에서 **분야 제목의 단락 스타일(ParagraphStyle)을 식별**한다. 분야 제목(심리/국어/고고학/미술사…)은 보통 본문과 다른 고유 스타일로 조판돼 있다.
2. Story/Spread를 **레이아웃 순서대로 순회**하며, 분야 제목 스타일 단락을 만나면 `현재_분야`를 갱신한다.
3. 이후 등장하는 책들에 `현재_분야`를 부여한다(다음 분야 제목을 만날 때까지).
4. **부서(department)와 분야(category)를 둘 다** 기록한다.

### 적용 범위

- `category` 칼럼(원본 분류, `category_src`)만 백필. 기존 882권·다른 칼럼은 유지.
- **`admin_overrides`는 건드리지 않는다.** 관리자가 수정한 분류가 있으면 `books_effective` 뷰의 `COALESCE`로 그대로 우선 적용된다.
- 백필 후 `books_effective`를 통해 웹 필터·섹션1 트리(주제=분야)에 자동 반영되는지 확인.

---

## 실행 순서

1. **API 한 권 검증** (9788956025513 → `subInfo.toc` 확인)
2. 통과 시 **전 권 API 재보강** (목차·상세·부가 필드)
3. **남은 빈 책만 스크래핑 폴백**(수정 B)
4. **IDML 추출기에 분야 인식 추가 → `category` 백필**(수정 C)
5. `books_effective` 뷰로 웹 반영 확인

---

## 검증 체크리스트

- [ ] 목차가 `toc` 칼럼에 채워짐(`_없음_` 아님)
- [ ] 책소개에 목차가 섞이지 않음
- [ ] 출판사 상세에 중복(더보기/접기) 없음
- [ ] 내비 보일러플레이트 제거됨
- [ ] `category`(분야)가 전 권 채워짐(심리·국어·고고학·미술사 등 노출)
- [ ] `admin_overrides` 보존됨(관리자 수정 분류가 그대로 유지)
- [ ] 웹 필터·섹션1 주제 목록에 분야가 반영됨
